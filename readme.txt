Info

App was initiated with create-react-app.
Besides default packages, included 'deepmerge' package.

Run

From command line in project directory: 'npm install' to install dependencies.
then run 'npm start' for dev view, or 'npm run build to get a build folder with index.html static page.

